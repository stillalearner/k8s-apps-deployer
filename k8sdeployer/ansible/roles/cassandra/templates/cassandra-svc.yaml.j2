apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ app_name }}
  name: {{ app_name }}
  namespace: {{ namespace }}
spec:
  clusterIP: None
  ports:
    - name: {{ app_name }}
      port: 9042
  selector:
    app: {{ app_name }}
